//repetir tareas. Loop.

for (let i = 1; i <= 5; i += 2) {
  console.log(i);
} //i+1 es igual que 1++. El for tiene 3 parámetros: el primero el contador
// o variable; el 2do es la condicion de corte; y el 3ero es en cuanto
//se va a incrementar el contador. en el i++, si quiero poner otro numero para
//incremente, es i+= (aca el numero al cual quiero que incremtne)

// i++  es lo mismo que  i = i + 1
// i+=2 es lo mismo que i = i + 2
// son abreviaciones
